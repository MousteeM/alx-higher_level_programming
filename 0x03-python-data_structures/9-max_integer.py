#!/usr/bin/python3

def max_integer(my_list=[]):
    if my_list = []:
        return None
    else:
        maxInt = my_list[0]
        for i in my_list:
            if my_list[i] > maxInt:
                maxInt = my_list[i]
        return maxInt
